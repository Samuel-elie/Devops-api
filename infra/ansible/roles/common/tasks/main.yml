- name: Update apt cache
  apt:
    update_cache: true

- name: Install common packages
  apt:
    name:
      - curl
      - git
      - unzip
      - wget
      - apt-transport-https
      - ca-certificates
      - gnupg
      - lsb-release
    state: present